name: Android CI Build (Qt 6.5.3) - Final Configuration

on:
  push:
    branches: [ "master" ]
  workflow_dispatch:

jobs:
  build-android:
    runs-on: ubuntu-latest
    
    # Zmienne środowiskowe
    env:
      QT_VERSION: 6.5.3 
      BUILD_DIR: build-android
      PLATFORM_TARGET: android
      ARCH_TARGET: android_arm64_v8a 
      CMAKE_VERBOSE_MAKEFILE: ON
      VERBOSE: 1

    steps:
    
    - name: Checkout code
      uses: actions/checkout@v4
      
    # Krok 1: Konfiguracja Javy
    - name: Set up Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    # Krok 2: Instalacja Qt, Android SDK i NDK (Jawne ustawienie narzędzi)
    - name: Install Qt and Android toolchain
      uses: jurplel/install-qt-action@v3
      with:
        version: ${{ env.QT_VERSION }}
        target: ${{ env.PLATFORM_TARGET }} 
        arch: ${{ env.ARCH_TARGET }}
        dir: ${{ runner.tool_cache }}
        cache: true 
        # Jawnie wymuszamy instalację narzędzi Androida
        tools: android
        
    # Krok 3: Przygotowanie katalogu roboczego
    - name: Create build directory
      run: mkdir -p ${{ env.BUILD_DIR }}
      
    # Krok 4: Konfiguracja projektu za pomocą CMake (Tryb set -e i ostateczna utwardzona ścieżka)
    - name: Configure CMake project for Android
      working-directory: ${{ env.BUILD_DIR }}
      shell: bash
      run: |
        # Włączenie trybu natychmiastowego wyjścia przy błędzie
        set -e
        
        # 1. Ustalanie ścieżek Qt (na podstawie logów V17)
        QT_ROOT_DIR="/opt/hostedtoolcache/Qt/${{ env.QT_VERSION }}/${{ env.ARCH_TARGET }}"
        QT_TOOLCHAIN_FILE="${QT_ROOT_DIR}/lib/cmake/Qt6/qt.toolchain.cmake"
        
        # 2. Ustalanie ścieżek Android NDK/SDK (Używamy $HOME, ponieważ tam są domyślnie instalowane)
        SDK_ROOT="/home/runner/Android/Sdk"
        NDK_DIR_BASE="${SDK_ROOT}/ndk"
        
        # Znajdź folder wersji NDK, jeśli nie znajdzie, NDK_VERSION_DIR będzie puste (set -e nas uchroni)
        # UWAGA: Użyjemy ls, które działało lepiej niż find
        NDK_VERSION_DIR=$(ls -d ${NDK_DIR_BASE}/*/ | head -n 1)
        
        # Usuwamy znak '/' na końcu ścieżki
        NDK_VERSION_DIR="${NDK_VERSION_DIR%/}"

        # Ustawienia NDK
        ANDROID_NDK_ROOT="${NDK_VERSION_DIR}"
        NDK_TOOLCHAIN_FILE="${ANDROID_NDK_ROOT}/build/cmake/android.toolchain.cmake"
        
        # 3. Ustawienie zmiennych środowiskowych
        export JAVA_HOME="$JAVA_HOME" 
        export ANDROID_SDK_ROOT="$SDK_ROOT"
        export ANDROID_NDK_ROOT="$ANDROID_NDK_ROOT"

        echo "QT_ROOT_DIR: $QT_ROOT_DIR"
        echo "ANDROID_NDK_ROOT: $ANDROID_NDK_ROOT"

        # 4. Sprawdzanie istnienia wszystkich krytycznych plików
        if [ ! -f "$QT_TOOLCHAIN_FILE" ]; then
          echo "BŁĄD: Nie znaleziono pliku toolchain Qt ($QT_TOOLCHAIN_FILE)."
          ls -F "/opt/hostedtoolcache/Qt/${{ env.QT_VERSION }}/" || true
          exit 1
        fi
        
        if [ ! -f "$NDK_TOOLCHAIN_FILE" ]; then
          echo "BŁĄD: Nie znaleziono pliku toolchain NDK ($NDK_TOOLCHAIN_FILE)."
          ls -F "$NDK_DIR_BASE" || true
          exit 1
        fi

        # 5. Konfiguracja CMake
        cmake .. \
          -G Ninja \
          -DCMAKE_TOOLCHAIN_FILE="$QT_TOOLCHAIN_FILE" \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_C_COMPILER=/usr/bin/gcc \
          -DCMAKE_CXX_COMPILER=/usr/bin/g++ \
          -DCMAKE_MAKE_PROGRAM="$(which ninja)" \
          -DQT_CHAINLOAD_TOOLCHAIN_FILE="$NDK_TOOLCHAIN_FILE"
          
    # Krok 5: Kompilacja i budowanie paczki Androida
    - name: Build Android package
      working-directory: ${{ env.BUILD_DIR }}
      run: cmake --build . --parallel $(nproc)
      
    # Krok 6: Upload wynikowego artefaktu (APK/AAB)
    - name: Upload Android Artifact (APK/AAB)
      uses: actions/upload-artifact@v4
      with:
        name: android-package-${{ env.QT_VERSION }}-${{ github.sha }}
        path: |
          ${{ env.BUILD_DIR }}/**/*.apk
          ${{ env.BUILD_DIR }}/**/*.aab
        retention-days: 7
